# Expenses Tracker Backend

Python 3.12+ FastAPI backend with SQLite storage.

## Setup

```bash
uv sync --extra dev
```

## Run

```bash
uv run uvicorn src.main:app --reload
```

Open http://localhost:8000 in a browser.

## Test

```bash
uv run pytest
```
